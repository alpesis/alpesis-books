##############################################################################
Collaboration
##############################################################################

to edit

::

    $ git pull
    $ git push
    
    $ git fetch
    $ git merge origin/master
    
    $ git commit -a

    # update the fork
    $ git remote add upstream <git://url/xx.git>
    $ git fetch upstream
    $ git pull upstream <master>
